server:
  port: 8021

spring:
  application:
    name: cloud-zuul-provider
eureka:
  client:
    service-url:
      defaultZone: http://eureka7001.com:4001/eureka
    fetch-registry: true
    register-with-eureka: true
