server:
  port: 8013

spring:
  application:
    name: cloud-hystrix-provider
eureka:
  client:
    service-url:
      defaultZone: http://eureka7001.com:4001/eureka
    fetch-registry: true
    register-with-eureka: true
